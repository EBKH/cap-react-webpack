{
	"name": "parent-app",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"dev": "cd priv/webpack-app && react-scripts start",
		"start": "cd priv/webpack-app && serve -s build",
		"heroku-postbuild": "cd priv/webpack-app && npm install && npm run build"
	}
}